@model IEnumerable<GestorDeTarefas.Models.SistemaProdutividade>

@{
    ViewData["Title"] = "Index";
}

<h1>Modelo Sistema de Produtividade</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>           
            @*<th>
                @Html.DisplayNameFor(model => model.Colaborador)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            
            @*<td>
                @Html.DisplayFor(modelItem => item.Colaborador.Name)
            </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.SistemaProdutividadeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SistemaProdutividadeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SistemaProdutividadeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
